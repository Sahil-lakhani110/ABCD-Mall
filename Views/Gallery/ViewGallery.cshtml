@model IEnumerable<Lakhani.Models.Gallery>

@{
    ViewData["Title"] = "ViewGallery";
    Layout = "~/Views/Shared/Admin.cshtml";
}

<div class="container-fluid">

    <!-- ===== PAGE HEADER ===== -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold">Manage Gallery</h2>
            <p class="text-muted mb-0">View, add, edit or delete Gallerys</p>
        </div>

        <a asp-action="Gallery" class="btn btn-primary">
            <i class="fa fa-plus"></i> Add New Gallery
        </a>
    </div>

    <!-- ===== TABLE CARD ===== -->
    <div class="card shadow-sm border-0">
        <div class="card-body">

            <div class="table-responsive">
                <table class="table table-hover align-middle">

                    <thead class="table-light">
                        <tr>
                            <th>Image</th>
                            <th>Title</th>
                            <th class="text-center">Actions</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <img src="~/ShopImage/@item.Image"
                                         class="rounded"
                                         style="width:80px; height:60px; object-fit:cover;" />
                                </td>

                                <td>@item.Title</td>

                                <td class="text-center">
                                    <a asp-action="Edit" asp-route-id="@item.Id"
                                       class="btn btn-sm btn-warning me-1">
                                        <i class="fa fa-edit"></i>
                                    </a>

                                    <a asp-action="Details" asp-route-id="@item.Id"
                                       class="btn btn-sm btn-info me-1 text-white">
                                        <i class="fa fa-eye"></i>
                                    </a>

                                    <a asp-action="Delete" asp-route-id="@item.Id"
                                       class="btn btn-sm btn-danger"
                                       onclick="return confirm('Do you want to delete this Gallery?');">
                                        <i class="fa fa-trash"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>

        </div>
    </div>

</div>