@model Lakhani.Models.FoodCourts

@{
    ViewData["Title"] = "Edit Food Court";
    Layout = "~/Views/Shared/Admin.cshtml";
}

<div class="container-fluid">

    <!-- ===== PAGE HEADER ===== -->
    <div class="mb-4">
        <h2 class="fw-bold">Edit Foodcourt</h2>
        <p class="text-muted">Update Foodcourt details and image</p>
    </div>

    <!-- ===== FORM CARD ===== -->
    <div class="card shadow-sm border-0">
        <div class="card-body">

            <form asp-action="Edit" enctype="multipart/form-data">

                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                <input type="hidden" asp-for="Id" />

                <div class="row">

                    <!-- Shop Name -->
                    <div class="col-md-6 mb-3">
                        <label asp-for="CounterName" class="form-label fw-semibold"></label>
                        <input asp-for="CounterName" class="form-control" />
                        <span asp-validation-for="CounterName" class="text-danger"></span>
                    </div>

                    <!-- Category -->
                    <div class="col-md-6 mb-3">
                        <label asp-for="ItemName" class="form-label fw-semibold"></label>
                        <input asp-for="ItemName" class="form-control" />
                        <span asp-validation-for="ItemName" class="text-danger"></span>
                    </div>

                    <!-- Description -->
                    <div class="col-md-12 mb-3">
                        <label asp-for="Price" class="form-label fw-semibold"></label>
                        <input type="number" step="0.01" asp-for="Price" rows="3" class="form-control" />
                        <span asp-validation-for="Price" class="text-danger"></span>
                    </div>

                    <!-- Current Image -->
                    <div class="col-md-6 mb-3">
                        <label class="form-label fw-semibold">Current Image</label><br />
                        <img src="~/ShopImage/@Model.ImagePath"
                             class="img-thumbnail shadow-sm"
                             style="width:150px; height:auto;" />
                    </div>

                    <!-- New Image Upload -->
                    <div class="col-md-6 mb-4">
                        <label asp-for="ImagePath" class="form-label fw-semibold">Change Image</label>
                        <input asp-for="ImagePath" name="Image" type="file" class="form-control" />
                        <span asp-validation-for="ImagePath" class="text-danger"></span>
                    </div>

                </div>

                <!-- Buttons -->
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-success">
                        <i class="fa fa-save"></i> Update Foodcourt
                    </button>

                    <a asp-action="ViewFoodCourt" class="btn btn-secondary">
                        <i class="fa fa-arrow-left"></i> Back to List
                    </a>
                </div>

            </form>

        </div>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}